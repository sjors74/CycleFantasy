trigger:
- master

pr:
- develop

pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'

stages:

- stage: BuildAndDeployAPI
  displayName: Build and Deploy API

  jobs:
  - job: BuildDeploy
    steps:

    - checkout: self
pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'

stages:

- stage: BuildAndTest
  displayName: Build and Test API

  jobs:
  - job: BuildTest
    steps:

    - checkout: self

    - task: UseDotNet@2
      inputs:
        packageType: 'sdk'
        version: '10.x'

    - script: dotnet restore CycleFantasy.sln
      displayName: Restore

    - script: dotnet build CycleFantasy.sln --configuration $(buildConfiguration) --no-restore
      displayName: Build

    - script: dotnet test CycleManager.Tests.Unit/CycleManager.Tests.Unit.csproj --configuration $(buildConfiguration) --no-build
      displayName: Run unit tests

- stage: DeployApi
  displayName: Deploy API
  dependsOn: BuildAndTest
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/master'))

  jobs:
  - job: Deploy
    steps:

    - script: >
        dotnet publish WebCycle/WebCycleApi.csproj
        -c $(buildConfiguration)
        -o $(Build.ArtifactStagingDirectory)/api
      displayName: Publish API

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(Build.ArtifactStagingDirectory)/api'
        includeRootFolder: false
        archiveType: zip
        archiveFile: '$(Build.ArtifactStagingDirectory)/api.zip'

    - task: AzureWebApp@1
      inputs:
        azureSubscription: 'azure-cyclefantasy-prod'
        appName: 'WebCycleApi20250508145015'
        package: '$(Build.ArtifactStagingDirectory)/api.zip'