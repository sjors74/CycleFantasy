@model TeamEditViewModel

<h3>Bewerk team</h3>
<hr />
<form asp-action="Edit">
    <div class="row">
        <!-- Linkerkant: Algemene gegevens -->
        <div class="col-md-6">
            <h4>Algemene gegevens</h4>
            <input type="hidden" asp-for="TeamId" />
            <div class="form-group mb-2">
                <label class="form-label" asp-for="CurrentTeamName"></label>
                <input asp-for="CurrentTeamName" class="form-control" />
            </div>
            <div class="form-group mb-2">
                <label class="form-label" asp-for="CountryId"></label>
                <select asp-for="CountryId" class="form-control" asp-items="Model.Countries"></select>
            </div>
            <div class="form-group mb-2">
                <label class="form-label" asp-for="PcsName"></label>
                <input asp-for="PcsName" class="form-control" />
            </div>
        </div>

        <!-- Rechterkant: TeamYears per jaar -->
        <div class="col-md-6">
            <h4>Per jaar</h4>
            <ul class="nav nav-tabs" id="yearTabs" role="tablist">
                @for (int i = 0; i < Model.AvailableYears.Count; i++)
                {
                    var seasonYear = Model.AvailableYears[i];
                    <li class="nav-item" role="presentation">
                        <button class="nav-link @(i == 0 ? "active" : "")"
                                id="tab-@seasonYear.SeasonYearId"
                                data-bs-toggle="tab"
                                data-bs-target="#year-@seasonYear.SeasonYearId"
                                type="button"
                                role="tab">
                            @seasonYear.Year
                        </button>
                    </li>
                }
            </ul>

            <div class="tab-content mt-3">
                @for (int i = 0; i < Model.AvailableYears.Count; i++)
                {
                    var seasonYear = Model.AvailableYears[i];
                    var teamYear = Model.TeamYears.FirstOrDefault(ty => ty.SeasonYearId == seasonYear.SeasonYearId);
                    <input type="hidden" name="AvailableYears.Index" value="@i" />
                    <input type="hidden" name="AvailableYears[@i].SeasonYearId" value="@seasonYear.SeasonYearId" />
                    <input type="hidden" name="AvailableYears[@i].Year" value="@seasonYear.Year" />
                    <div class="tab-pane fade @(i == 0 ? "show active" : "")" id="year-@seasonYear.SeasonYearId" role="tabpanel">
                        <div id="teamyear-container-@seasonYear.SeasonYearId">
                            @if (teamYear != null)
                            {
                                <input type="hidden" name="TeamYears.Index" value="@i" />
                                <input type="hidden" name="TeamYears[@i].TeamYearId" value="@teamYear.TeamYearId" />
                                <input type="hidden" name="TeamYears[@i].SeasonYearId" value="@teamYear.SeasonYearId" />


                                <div class="form-group mt-2">
                                    <label>Teamnaam</label>
                                    <div class="input-group">
                                        <input type="text" class="form-control" style="max-width: 300px;" name="TeamYears[@i].Name" value="@teamYear.Name" />
                                        <button type="button"
                                                class="btn btn-outline-danger"
                                                onclick="removeTeamYear(@seasonYear.SeasonYearId, @seasonYear.Year, @i)">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="form-group mt-2">
                                    <label>Teamnaam</label>
                                    <div class="input-group" style="max-width: 300px;">
                                        <input type="text" class="form-control" value="Geen team aanwezig voor @seasonYear.Year" readonly />
                                        <button type="button" class="btn btn-outline-primary"
                                                onclick="createTeamYear(@seasonYear.SeasonYearId, @i)">
                                            <i class="bi bi-plus"></i>
                                        </button>
                                    </div>                     
                                </div>
                            }
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
    <div class="mt-3 d-flex justify-content-between">
        <a asp-action="Index" class="btn btn-outline-secondary">Terug naar de lijst</a>
        <button type="submit" class="btn btn-primary">Opslaan</button>
    </div>
</form>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        function createTeamYear(seasonYearId, index) {
            var container = document.getElementById('teamyear-container-' + seasonYearId);
            container.innerHTML = `
                <input type="hidden" name="TeamYears.Index" value="${index}" />
                <input type="hidden" name="TeamYears[${index}].SeasonYearId" value="${seasonYearId}" />
                <div class="form-group">
                    <label>Teamnaam</label>
                    <input type="text" class="form-control" name="TeamYears[${index}].Name" value="" />
                </div>
            `;
        }
    </script>
    <script>
        function removeTeamYear(seasonYearId, year, index) {
            var container = document.getElementById('teamyear-container-' + seasonYearId);
            container.innerHTML = `
                <div class="form-group mt-2">
                    <label>Teamnaam</label>
                    <div class="input-group" style="max-width: 300px;">
                        <input type="text" class="form-control" value="Geen team aanwezig voor ${year}" readonly />
                        <button type="button" class="btn btn-outline-primary"
                            onclick="createTeamYear(${seasonYearId}, ${year}, ${index})">
                            <i class="bi bi-plus"></i>
                        </button>
                    </div>
                </div>
            `;
        }
    </script>
}
